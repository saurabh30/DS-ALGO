class Tree
{
    void preorder(Node root)
    {
       // Your code goes here
       if(root == null)
        return;
       System.out.print(root.data+" ");
       preorder(root.left);
       preorder(root.right);
    }
}
