class Tree
{
    void inOrder(Node root)
    {
       // Your code goes here
       if(root == null)
         return;
       inOrder(root.left);
       System.out.print(root.data+ " ");
       inOrder(root.right);
    }
}
